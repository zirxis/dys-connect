# نظام تسجيل الدخول والحسابات - DYS-CONNECT

## 📋 مرحباً بك في نظام المصادقة المتقدم

تم بناء نظام تسجيل دخول متكامل يشمل حسابات لمختلف الأدوار مع ميزة الربط بين الحسابات.

---

## 🔐 الحسابات المتاحة

### 👩‍👦 **حسابات الأمهات (3 حسابات)**
| اسم المستخدم | كلمة المرور | الاسم |
|---|---|---|
| `fatima_01` | `pass123` | فاطمة الزهراء |
| `layla_02` | `pass123` | ليلى محمد |
| `aisha_03` | `pass123` | عائشة أحمد |

### 🧑‍🏫 **حسابات المعلمين (3 حسابات)**
| اسم المستخدم | كلمة المرور | الاسم |
|---|---|---|
| `ahmed_teacher` | `pass123` | أحمد محمد علي |
| `sara_teacher` | `pass123` | سارة حسن |
| `mohsen_teacher` | `pass123` | محسن كريم |

### 🧠 **حسابات المختصين (3 حسابات)**
| اسم المستخدم | كلمة المرور | الاسم |
|---|---|---|
| `dr_amina` | `pass123` | د. أمينة محمود |
| `dr_karim` | `pass123` | د. كريم ياسين |
| `dr_huda` | `pass123` | د. هدى فرحات |

### 🏫 **حسابات المؤسسات (3 حسابات)**
| اسم المستخدم | كلمة المرور | الاسم |
|---|---|---|
| `school_algiers` | `pass123` | مدرسة النور الابتدائية |
| `school_oran` | `pass123` | مدرسة الفردوس الابتدائية |
| `school_constantine` | `pass123` | مدرسة الهلال الابتدائية |

### 🎓 **حسابات الباحثين (3 حسابات)**
| اسم المستخدم | كلمة المرور | الاسم |
|---|---|---|
| `dr_youssef` | `pass123` | د. يوسف حميد |
| `prof_fatima` | `pass123` | أ.د. فاطمة خياط |
| `prof_ali` | `pass123` | أ.د. علي سعيد |

---

## 🚀 كيفية البدء

### 1. الدخول إلى صفحة تسجيل الدخول
```
اذهب إلى: login.html
```

### 2. اختيار حساب تجريبي
- يمكنك النقر على أي حساب من قائمة الحسابات التجريبية
- سيتم ملء بيانات الدخول تلقائياً
- اضغط "تسجيل الدخول"

### 3. الدخول إلى لوحة التحكم
- ستظهر لوحة تحكم مخصصة لكل دور
- يمكنك رؤية المستخدمين المرتبطين بك

---

## 🔗 نظام الربط بين الحسابات

### الأم (Mother) يمكنها رؤية:
- ✅ المختصون (Specialists)
- ✅ الباحثون (Researchers)
- ✅ المعلمون (Teachers)

### المعلم (Teacher) يمكنه رؤية:
- ✅ الأمهات (Mothers)
- ✅ المختصون (Specialists)
- ✅ الباحثون (Researchers)
- ✅ المؤسسات (Institutions)

### المختص (Specialist) يمكنه رؤية:
- ✅ الأمهات (Mothers)
- ✅ المعلمون (Teachers)
- ✅ الباحثون (Researchers)
- ✅ المؤسسات (Institutions)

### المؤسسة (Institution) يمكنها رؤية:
- ✅ المعلمون (Teachers)
- ✅ المختصون (Specialists)
- ✅ الباحثون (Researchers)

### الباحث (Researcher) يمكنه رؤية:
- ✅ الأمهات (Mothers)
- ✅ المعلمون (Teachers)
- ✅ المختصون (Specialists)
- ✅ المؤسسات (Institutions)

---

## 📁 الملفات المهمة

### `login.html` 🔑
صفحة تسجيل الدخول المخصصة مع:
- واجهة جميلة وسهلة الاستخدام
- قائمة الحسابات التجريبية
- تحقق من بيانات الدخول
- تأثيرات بصرية احترافية

### `auth.js` ⚙️
نظام المصادقة الرئيسي يتضمن:
- قاعدة البيانات المحلية للحسابات
- نظام الربط بين الأدوار
- دوال المصادقة والتحقق
- دوال البحث والتواصل

### `dashboard.html` 📊
لوحة التحكم المحدثة مع:
- التحقق من تسجيل الدخول
- عرض المستخدمين المرتبطين
- قائمة محلية في الملف الشخصي
- خيار تسجيل الخروج

### `main.js` 🎯
ملف JavaScript الرئيسي المحدث لتوجيه المستخدمين لصفحة تسجيل الدخول

---

## 🎯 الميزات الرئيسية

### 1. نظام المصادقة الآمن
```javascript
// التحقق من بيانات المستخدم
const user = usersDatabase[username];
if (user && user.password === password) {
    saveUserSession(username, user);
}
```

### 2. حفظ الجلسة
```javascript
// حفظ بيانات المستخدم في localStorage
localStorage.setItem('currentUser', JSON.stringify(sessionData));
```

### 3. الربط الديناميكي بين الحسابات
```javascript
// الحصول على المستخدمين المتصلين
const connectedUsers = getConnectedUsers(currentRole);
```

### 4. البحث والتصفية
```javascript
// البحث عن مستخدمين معينين
const results = searchUsers(query, connectedRolesOnly);
```

---

## 🔄 سير العمل

```
1. المستخدم يختار دوره من الصفحة الرئيسية
        ↓
2. يتم نقله إلى صفحة تسجيل الدخول (login.html)
        ↓
3. يدخل بيانات الدخول (أو ينقر على حساب تجريبي)
        ↓
4. يتم التحقق من البيانات في auth.js
        ↓
5. إذا كانت صحيحة، يتم حفظ الجلسة في localStorage
        ↓
6. يتم نقل المستخدم إلى dashboard.html
        ↓
7. يرى المستخدمين المرتبطين به
        ↓
8. يمكنه التواصل معهم
```

---

## 💾 كيفية إضافة حساب جديد

أضف حساباً جديداً إلى `auth.js`:

```javascript
'username_new': {
    password: 'password123',
    role: 'mother',  // الدور
    name: 'الاسم الكامل',
    email: 'email@example.com',
    photo: '👩‍👦',  // الرمز
    // بيانات إضافية حسب الدور
}
```

---

## 🧪 اختبار النظام

### اختبر الدخول:
1. افتح `login.html`
2. استخدم أحد الحسابات المذكورة أعلاه
3. تأكد من ظهور لوحة التحكم الخاصة بالدور

### اختبر الربط:
1. سجل الدخول كـ "أم"
2. ستظهر بطاقات المختصين والباحثين والمعلمين
3. انقر على "تواصل" للتواصل معهم

### اختبر تسجيل الخروج:
1. انقر على اسمك في الأعلى يمين الصفحة
2. اختر "تسجيل الخروج"
3. تأكد من عودتك لصفحة تسجيل الدخول

---

## 📞 الدوال المتاحة في `auth.js`

```javascript
// التحقق من المصادقة
checkAuthentication()

// الحصول على المستخدم الحالي
getCurrentUser()

// تسجيل الخروج
logout()

// الحصول على المستخدمين المتصلين
getConnectedUsers(currentRole)

// الحصول على جميع المستخدمين من دور معين
getUsersByRole(role)

// البحث عن مستخدمين
searchUsers(query, connectedRolesOnly)

// الحصول على معلومات مستخدم
getUserInfo(username)

// التحقق من إمكانية الوصول
canAccess(fromRole, toRole)
```

---

## 🎨 التصميم والألوان

- **اللون الأساسي**: #C41E3A (أحمر أساسي)
- **لون ثانوي**: #E85A4F (أحمر ثانوي)
- **لون البطاقات**: #FDF6E3 (كريمي ناعم)

---

## ✨ ملاحظات مهمة

1. **البيانات محلية**: جميع البيانات مخزنة في localStorage (لا توجد خادم)
2. **الحسابات تجريبية**: يمكنك إضافة حسابات حقيقية في المستقبل
3. **الأمان**: في بيئة الإنتاج، استخدم خادماً آمناً مع تشفير كلمات المرور
4. **التوافقية**: يعمل مع جميع المتصفحات الحديثة

---

## 🚀 الخطوات القادمة

1. إضافة قاعدة بيانات حقيقية (Backend)
2. تشفير كلمات المرور
3. إضافة تحقق من البريد الإلكتروني
4. إضافة نظام الإشعارات
5. إضافة نظام الرسائل المتقدم

---

**تم إنشاؤه بواسطة**: GitHub Copilot
**التاريخ**: 29 نوفمبر 2025
**الإصدار**: 1.0.0
